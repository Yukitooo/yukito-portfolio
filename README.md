# Zachary Brent Mayuga — Portfolio

Personal software developer portfolio built with SvelteKit and TypeScript.

## Development

```sh
npm install
npm run dev
```

## Quality checks

```sh
npm run format
npm run check
npm run lint
npm run build
```

Set `PUBLIC_SITE_URL` to the deployed origin so canonical and Open Graph URLs are absolute in production.
